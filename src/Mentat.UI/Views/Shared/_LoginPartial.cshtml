@using Microsoft.AspNetCore.Identity
@using Mentat.UI.Areas.Identity.Data

@inject SignInManager<MentatUser> SignInManager
@inject UserManager<MentatUser> UserManager

<ul class="navbar-nav flex-column">
@if (SignInManager.IsSignedIn(User))
{
    <!--<li class="nav-item">
        <a id="UserProfile" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/UserProfile" title="View Profile">@UserManager.GetUserName(User)</a>
    </li>-->
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button style="position:relative;left:45px" id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
    <div class="row">
        <div class="col-4">
            <li class="nav-item">
                 <a style="position:relative;left:15px" class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
            </li>
        </div>
        <div class="col-4">
            <li class="nav-item">
                <a style="position:relative;left:10px" class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>
        </div>
    </div>
}
</ul>
